*.backup
.terraform/*
terraform.tfstate
.terraform.lock.hcl
.terraform.tfstate.lock.info
inventory.ini
# prevent pushing of cloud-provider auth/terraform config
# if you need to not ignore a file in that folder, use !<file name> (ex: !test.json) 
credentials/*

